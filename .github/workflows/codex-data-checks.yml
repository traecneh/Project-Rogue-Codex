name: Codex Data Checks

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    name: Tests and validation
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Python dependencies
        run: python -m pip install cryptography pillow

      - name: Install browser smoke dependencies
        run: npm ci

      - name: Run unit tests
        run: |
          set +e
          output_file="$RUNNER_TEMP/unittest-output.txt"
          python -m unittest discover -s tests -v 2>&1 | tee "$output_file"
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            {
              echo "### Unit test failure output"
              echo '```'
              tail -n 80 "$output_file"
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            message=$(tail -n 25 "$output_file" | sed ':a;N;$!ba;s/%/%25/g;s/\r/%0D/g;s/\n/%0A/g')
            echo "::error title=Unit tests failed::$message"
          fi
          exit "$status"

      - name: Run release readiness check
        run: python -m tools.codex_pipeline release-check

      - name: Smoke test site pages
        run: python -m tools.codex_pipeline smoke-site --smoke-timeout-ms 60000

      - name: Check whitespace
        run: git diff --check
